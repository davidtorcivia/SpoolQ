# Mutation testing configuration.
# Most defensive error paths are now covered by targeted tests.
# Remaining exclusions are for paths that require filesystem fault injection
# (swapping files between verify and rename, etc.) which will be addressed
# by the FsOps/Clock seam (audit cross-cutting improvement #2).
exclude_re = [
    "ack_impl.*dest_stat.*vf",
]
