[package]
name = "steadq-fs-linux"
description = "Linux syscall substrate for SteadQ/1"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

[dependencies]
libc = "0.2"

[features]
default = []
# Historical alias. Fault injection is always compiled; arming is a no-op
# until tests call fault::inject.
fault-injection = []
